import React from "react";
import ReactDOM from "react-dom";
import All from "./All";

ReactDOM.render(
  <React.StrictMode>
    <All />
  </React.StrictMode>,
  document.getElementById("root")
);
